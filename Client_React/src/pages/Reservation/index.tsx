import React from "react";
import BookATable from "./BookATable.tsx";

const ReservationPage = (): React.ReactNode => {
  return (
    <main>
      <BookATable />
    </main>
  );
};

export default ReservationPage;
